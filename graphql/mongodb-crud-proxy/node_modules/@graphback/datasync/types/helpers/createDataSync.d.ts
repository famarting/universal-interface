import { Db } from 'mongodb';
import { GraphQLSchema } from 'graphql';
import { GraphbackAPIConfig, GraphbackAPI } from "graphback";
import { GlobalConflictConfig } from "../util";
declare type DataSyncGraphbackAPIConfig = Omit<GraphbackAPIConfig, "dataProviderCreator">;
/**
 * Config to create a Graphback DataSync API
 */
export interface DataSyncAPIConfig {
    /**
     * MongoDB driver
     */
    db: Db;
    /**
     * Conflict configmap for the data models
     */
    conflictConfig?: GlobalConflictConfig;
    /**
     * GraphbackAPI config
     */
    graphbackAPIConfig?: DataSyncGraphbackAPIConfig;
}
export declare function createDataSyncAPI(model: string | GraphQLSchema, createDataSyncConfig: DataSyncAPIConfig): GraphbackAPI;
export {};
//# sourceMappingURL=createDataSync.d.ts.map