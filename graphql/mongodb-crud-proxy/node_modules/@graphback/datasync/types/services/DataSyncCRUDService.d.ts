import { CRUDService, CRUDServiceConfig, ModelDefinition } from '@graphback/core';
import { GraphQLResolveInfo } from 'graphql';
import { DataSyncProvider } from "../providers";
export interface SyncList<T> {
    items: T[];
    lastSync: Date;
    limit: number;
}
/**
 * CRUD Service for datasync
 */
export declare class DataSyncCRUDService<T = any> extends CRUDService<T> {
    constructor(model: ModelDefinition, db: DataSyncProvider, config: CRUDServiceConfig);
    /**
     * sync
     * For delta queries
     */
    sync(lastSync: Date, info?: GraphQLResolveInfo, filter?: any, limit?: number): Promise<SyncList<T>>;
}
//# sourceMappingURL=DataSyncCRUDService.d.ts.map